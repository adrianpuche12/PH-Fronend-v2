/**
 * Punto de entrada del sistema de diseño.
 * Importar siempre desde aquí, nunca directo desde tokens.ts
 *
 *   import { COLOR, SPACE, RADIUS, FONT_SIZE, SHADOW, CONTROL } from '../theme';
 */

export {
  COLOR,
  FONT_FAMILY,
  FONT_SIZE,
  FONT_WEIGHT,
  LINE_HEIGHT,
  SPACE,
  RADIUS,
  SHADOW,
  CONTROL,
  Z,
  MOTION,
  BREAKPOINT,
  PAPER_THEME,
  STYLE,
} from './tokens';
